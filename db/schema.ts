import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const appState = sqliteTable("app_state", { id: integer("id").primaryKey(), xp: integer("xp").notNull().default(120), streak: integer("streak").notNull().default(3), completed: text("completed").notNull().default("[]"), journal: text("journal").notNull().default("[]"), goals: text("goals").notNull().default("[]"), updatedAt: text("updated_at").notNull() });
export const userState = sqliteTable("user_state", { userId:text("user_id").primaryKey(), xp:integer("xp").notNull().default(120), streak:integer("streak").notNull().default(3), completed:text("completed").notNull().default("[]"), journal:text("journal").notNull().default("[]"), goals:text("goals").notNull().default("[]"), updatedAt:text("updated_at").notNull() });
